# This file was generated by FRUT's Jucer2CMake from "NewProject.jucer"

cmake_minimum_required(VERSION 3.5)

project("Project1")

if (CMAKE_SYSTEM_NAME MATCHES "Windows")

  # The file structure has to be:
  # ---- FRUT
  #   |- JUCE
  #   |- asiosdk_2.3.3_2019-06-14
  #   |- [This Project]
  list(APPEND CMAKE_MODULE_PATH "${CMAKE_CURRENT_LIST_DIR}/../../FRUT/prefix/FRUT/cmake")
  set(JUCE_MODULES_GLOBAL_PATH "${CMAKE_CURRENT_LIST_DIR}/../../JUCE/modules")
  set(ASIO_PATH "${CMAKE_CURRENT_LIST_DIR}/../../asiosdk_2.3.3_2019-06-14/common")
  add_compile_definitions(WIN)

elseif (CMAKE_SYSTEM_NAME MATCHES "Darwin")

  # This is my computer specific setting
  list(APPEND CMAKE_MODULE_PATH "/Users/dfpmts/FRUT/prefix/FRUT/cmake")  
  set(JUCE_MODULES_GLOBAL_PATH "/Users/dfpmts/JUCE/modules/")
  
else()

  # Abort!
  message(FATAL_ERROR "Unknown platform, Abort.")

endif()


include(Reprojucer)


set(Project1_jucer_FILE
  "${CMAKE_CURRENT_LIST_DIR}/../NewProject.jucer"
)

# FRUT use PROJECT_FILE to locate the project folder, that why we can just write "Include/..." as below

jucer_project_begin(
  JUCER_FORMAT_VERSION "1"
  PROJECT_FILE "${Project1_jucer_FILE}"
  PROJECT_ID "EEYu1l"
)

jucer_project_settings(
  PROJECT_NAME "Project1"
  PROJECT_VERSION "1.0.0"
  USE_GLOBAL_APPCONFIG_HEADER OFF
  ADD_USING_NAMESPACE_JUCE_TO_JUCE_HEADER OFF
  DISPLAY_THE_JUCE_SPLASH_SCREEN ON # Required for closed source applications without an Indie or Pro JUCE license
  PROJECT_TYPE "Console Application"
  BUNDLE_IDENTIFIER "com.yourcompany.Project1"
  CXX_LANGUAGE_STANDARD "C++17"
  HEADER_SEARCH_PATHS ${ASIO_PATH} "${CMAKE_CURRENT_LIST_DIR}/../Include/"
)


# --------------------------------Include---------------------------------- #

jucer_project_files("Project1/Include"
# Compile   Xcode     Binary    File
#           Resource  Resource
  .         .         .         "Include/Config.hpp"  
  .         .         .         "Include/RingBuffer.hpp"
  .         .         .         "Include/SyncQueue.hpp"
  .         .         .         "Include/PHY_Sender.hpp"
  .         .         .         "Include/PHY_Receiver.hpp"
  .         .         .         "Include/PHY_FrameExtractor.hpp"
  .         .         .         "Include/PHY_Layer.hpp"
  .         .         .         "Include/LT_Send.hpp"
)

# --------------------------------Source----------------------------------- #

jucer_project_files("Project1/Source"
# Compile   Xcode     Binary    File
#           Resource  Resource
  x         .         .         "Source/Project1.cpp"  
)

# ------------------------------------------------------------------------- #

jucer_project_module(
  juce_audio_basics
  PATH "${JUCE_MODULES_GLOBAL_PATH}"
)

jucer_project_module(
  juce_audio_devices
  PATH "${JUCE_MODULES_GLOBAL_PATH}"
  # JUCE_USE_WINRT_MIDI
  JUCE_ASIO ON
  JUCE_WASAPI OFF
  JUCE_DIRECTSOUND OFF
  JUCE_ALSA OFF
  # JUCE_JACK
  # JUCE_BELA
  JUCE_USE_ANDROID_OBOE OFF
  # JUCE_USE_OBOE_STABILIZED_CALLBACK
  # JUCE_USE_ANDROID_OPENSLES
  # JUCE_DISABLE_AUDIO_MIXING_WITH_OTHER_APPS
)

jucer_project_module(
  juce_audio_formats
  PATH "${JUCE_MODULES_GLOBAL_PATH}"
  # JUCE_USE_FLAC
  # JUCE_USE_OGGVORBIS
  # JUCE_USE_MP3AUDIOFORMAT
  # JUCE_USE_LAME_AUDIO_FORMAT
  # JUCE_USE_WINDOWS_MEDIA_FORMAT
)

jucer_project_module(
  juce_audio_processors
  PATH "${JUCE_MODULES_GLOBAL_PATH}"
  # JUCE_PLUGINHOST_VST
  # JUCE_PLUGINHOST_VST3
  # JUCE_PLUGINHOST_AU
  # JUCE_PLUGINHOST_LADSPA
  # JUCE_PLUGINHOST_LV2
  # JUCE_PLUGINHOST_ARA
  # JUCE_CUSTOM_VST3_SDK
)

jucer_project_module(
  juce_audio_utils
  PATH "${JUCE_MODULES_GLOBAL_PATH}"
  # JUCE_USE_CDREADER
  # JUCE_USE_CDBURNER
)

jucer_project_module(
  juce_core
  PATH "${JUCE_MODULES_GLOBAL_PATH}"
  # JUCE_FORCE_DEBUG
  # JUCE_LOG_ASSERTIONS
  # JUCE_CHECK_MEMORY_LEAKS
  # JUCE_DONT_AUTOLINK_TO_WIN32_LIBRARIES
  # JUCE_INCLUDE_ZLIB_CODE
  # JUCE_USE_CURL
  # JUCE_LOAD_CURL_SYMBOLS_LAZILY
  # JUCE_CATCH_UNHANDLED_EXCEPTIONS
  # JUCE_ALLOW_STATIC_NULL_VARIABLES
  JUCE_STRICT_REFCOUNTEDPOINTER ON
  # JUCE_ENABLE_ALLOCATION_HOOKS
)

jucer_project_module(
  juce_data_structures
  PATH "${JUCE_MODULES_GLOBAL_PATH}"
)

jucer_project_module(
  juce_events
  PATH "${JUCE_MODULES_GLOBAL_PATH}"
  # JUCE_EXECUTE_APP_SUSPEND_ON_BACKGROUND_TASK
)

jucer_project_module(
  juce_graphics
  PATH "${JUCE_MODULES_GLOBAL_PATH}"
  # JUCE_USE_COREIMAGE_LOADER
  # JUCE_USE_DIRECTWRITE
  # JUCE_DISABLE_COREGRAPHICS_FONT_SMOOTHING
)

jucer_project_module(
  juce_gui_basics
  PATH "${JUCE_MODULES_GLOBAL_PATH}"
  # JUCE_ENABLE_REPAINT_DEBUGGING
  # JUCE_USE_XRANDR
  # JUCE_USE_XINERAMA
  # JUCE_USE_XSHM
  # JUCE_USE_XRENDER
  # JUCE_USE_XCURSOR
  # JUCE_WIN_PER_MONITOR_DPI_AWARE
)

jucer_project_module(
  juce_gui_extra
  PATH "${JUCE_MODULES_GLOBAL_PATH}"
  # JUCE_WEB_BROWSER
  # JUCE_USE_WIN_WEBVIEW2
  # JUCE_ENABLE_LIVE_CONSTANT_EDITOR
)

if (CMAKE_SYSTEM_NAME MATCHES "Windows")

  jucer_export_target(
    "Visual Studio 2022"
    EXTRA_COMPILER_FLAGS
    "/bigobj"
  )

  jucer_export_target_configuration(    
    "Visual Studio 2022"
    NAME "Debug"
    DEBUG_MODE ON
  )

  jucer_export_target_configuration(
    "Visual Studio 2022"
    NAME "Release"
    DEBUG_MODE OFF
  )

elseif (CMAKE_SYSTEM_NAME MATCHES "Darwin")

  jucer_export_target(
    "Xcode (macOS)"
  )

  jucer_export_target_configuration(
    "Xcode (macOS)"
    NAME "Debug"
    DEBUG_MODE ON
    BINARY_NAME "Project1"
  )

  jucer_export_target_configuration(
    "Xcode (macOS)"
    NAME "Release"
    DEBUG_MODE OFF
    BINARY_NAME "Project1"
  )

endif()

jucer_project_end()
